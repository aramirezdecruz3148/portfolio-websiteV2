//this will probably want to be a functional reusable
//component that takes a src, text
//will need to style the text ontop of the photo
//look at Moddo for a reference 
import React from 'react';
import PropTypes from 'prop-types';

function AboutMePics() {
  return ()
}

AboutMePics.propTypes = {

};

export default AboutMePics;
